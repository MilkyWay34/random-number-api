{
    "name": "app/random-number-api",
    "description": "REST API for random number generation (Pure PHP, DDD, Modular Architecture)",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
